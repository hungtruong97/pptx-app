import { TextComponent } from "../../types/componentTypes";

export const pageNumberStyles: Record<string, TextComponent> = {
  default: {
    align: "center",
    valign: "middle",
    fontSize: 8,
    bold: false,
  },
};
