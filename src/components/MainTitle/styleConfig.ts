import { TextComponent } from "../../types/componentTypes";

export const mainTitleStyles: Record<string, TextComponent> = {
  default: {
    align: "center",
    fontSize: 48,
    bold: true,
  },
};
