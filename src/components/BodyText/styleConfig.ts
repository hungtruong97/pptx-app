import { TextComponent } from "../../types/componentTypes";

export const bodyTextStyles: Record<string, TextComponent> = {
  default: {
    align: "left",
    valign: "top",
    fontSize: 12,
    bold: false,
  },
};
