/**
 * Converts inches to centimeters.
 * @param {number} cm - The measurement in cm.
 * @returns {number} - The measurement converted to inches.
 */
export function cmToInches(cm: number): number {
  return cm / 2.54;
}
